[provider]
name = "python"

[start]
cmd = "gunicorn my_agent.web.server:app -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT --workers 1 --timeout 120"

[install]
cmd = "pip install -r requirements.txt"
